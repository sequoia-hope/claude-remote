# Optional: Anthropic API key (not needed if you use `claude login` — auth is
# persisted via the ~/.claude bind mount)
# ANTHROPIC_API_KEY=sk-ant-...

# Tailscale IP — auto-detected by run.sh, override only if needed
# TAILSCALE_IP=100.x.x.x

# Project directory to mount (absolute path)
PROJECT_DIR=/home/youruser/projects/my-project

# Optional: ttyd basic auth (off by default — Tailscale handles access)
# TTYD_USER=claude
# TTYD_PASS=your-secure-password

# Optional: tmux session name (useful for multiple instances)
SESSION_NAME=claude
